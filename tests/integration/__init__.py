"""Integration tests: Controller + Bridge + ESP32 emulator, no hardware.

Nothing here needs a board, a port or Isaac.  The emulator is the transport, and
the rest of the loop is the code that will run on the rover.
"""
